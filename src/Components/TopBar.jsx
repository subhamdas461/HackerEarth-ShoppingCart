import React from "react";

const TopBar = () => {
    const styles = {
        height: "60px",
        background: "var(--light-shade)",
    };
    return <div style={styles} className="top-bar"></div>;
};

export default TopBar;
